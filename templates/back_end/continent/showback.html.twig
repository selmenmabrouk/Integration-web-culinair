{% extends 'back_end/base.html.twig' %}

{% block title %}Continent{% endblock %}

{% block body %}
    <h1>Continent</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ continent.id }}</td>
        </tr>
        <tr>
            <th>NomContinent</th>
            <td>{{ continent.nomContinent }}</td>
        </tr>
        </tbody>
    </table>

    <a href="{{ path('continent_indexback') }}">back to list</a>

    <a href="{{ path('continent_editback', {'id': continent.id}) }}">edit</a>

    {{ include('back_end/continent/_delete_form.html.twig') }}
{% endblock %}
