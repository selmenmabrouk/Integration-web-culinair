{% extends 'back_end/base.html.twig' %}

{% block title %}Transport{% endblock %}

{% block body %}

<div class="content-page">
    <!-- Start content -->
    <div class="content">
        <div class="container-fluid">
            {% for message in app.flashes('success') %}
                <div class="alert alert-success">
                    {{ message }}
                </div>
            {% endfor %}
            <div class="row">
                <div class="col-12">
                    <div class="card-box table-responsive">
    <h1>Transport</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ transport.id }}</td>
            </tr>
            <tr>
                <th>Duree_transport</th>
                <td>{{ transport.dureeTransport }}</td>
            </tr>
            <tr>
                <th>Type_transport</th>
                <td>{{ transport.typeTransport }}</td>
            </tr>
            <tr>
                <th>Capacite</th>
                <td>{{ transport.capacite }}</td>
            </tr>
            <tr>
                <th>nombre_participant</th>

            </tr>
        </tbody>
    </table>

    <a href="{{ path('transport_index') }}">back to list</a>

    <a href="{{ path('transport_edit', {'id': transport.id}) }}">edit</a>

    {{ include('back_end/transport/_delete_form.html.twig') }}
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
